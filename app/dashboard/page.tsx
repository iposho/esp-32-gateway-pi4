import { Dashboard } from '@/components/dashboard/dashboard'

export const dynamic = 'force-dynamic'

export const metadata = {
  title: 'Панель управления — ESP32 Gateway',
}

export default function DashboardPage() {
  return <Dashboard />
}
